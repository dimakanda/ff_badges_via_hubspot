class FfBadges::Badges::<%= name %>Observer < ActiveRecord::Observer

  # observe :foo TODO: set propper model to observe
  
  # TODO: set propper conditions here
  #def after_update(foo)
  #  user = foo.user
  #
  #  if badge = Badge.where(name: '<%= name -%>').first
  #   user.earn_badge!(badge) if user.deserves_badge?(badge)
  #  <% if invertable %>conditions for remove badge here
  #   user.remove_badge!(badge)<% end %>
  #  end
  #end

end